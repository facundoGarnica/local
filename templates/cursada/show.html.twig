{% extends 'base.html.twig' %}

{% block title %}Cursada{% endblock %}

{% block body %}
    <h1>Cursada</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cursada.id }}</td>
            </tr>
            <tr>
                <th>Condicion</th>
                <td>{{ cursada.condicion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cursada_index') }}">back to list</a>

    <a href="{{ path('app_cursada_edit', {'id': cursada.id}) }}">edit</a>

    {{ include('cursada/_delete_form.html.twig') }}
{% endblock %}
