{% extends 'base.html.twig' %}

{% block title %}Revista index{% endblock %}

{% block body %}
    <h1>Listado de Revistas</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Descripción</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for revistum in revistas %}
            <tr>
                <td>{{ revistum.id }}</td>
                <td>{{ revistum.descripcion }}</td>
                <td>
                    <a href="{{ path('app_revista_show', {'id': revistum.id}) }}">Mostrar</a>
                    <a href="{{ path('app_revista_edit', {'id': revistum.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">No se encontraron registros</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_revista_new') }}">Crear Nueva Revista</a>
{% endblock %}
