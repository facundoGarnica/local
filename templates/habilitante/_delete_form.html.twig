<form method="post" action="{{ path('app_habilitante_delete', {'id': habilitante.id}) }}" onsubmit="return confirm('Confirmar Eliminación');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ habilitante.id) }}">
    <button class="btn">Eliminar</button>
</form>
