<form method="post" action="{{ path('app_asignatura_delete', {'id': asignatura.id}) }}" onsubmit="return confirm('Confirmar Eliminación');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asignatura.id) }}">
    <button class="btn">Eliminar</button>
</form>
