{% extends 'base.html.twig' %}

{% block title %}Nota{% endblock %}

{% block body %}
    <h1>Nota</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notum.id }}</td>
            </tr>
            <tr>
                <th>Parcial</th>
                <td>{{ notum.parcial }}</td>
            </tr>
            <tr>
                <th>Recuperatorio1</th>
                <td>{{ notum.recuperatorio1 }}</td>
            </tr>
            <tr>
                <th>Parcial2</th>
                <td>{{ notum.parcial2 }}</td>
            </tr>
            <tr>
                <th>Recuperatorio2</th>
                <td>{{ notum.recuperatorio2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nota_index') }}">back to list</a>

    <a href="{{ path('app_nota_edit', {'id': notum.id}) }}">edit</a>

    {{ include('nota/_delete_form.html.twig') }}
{% endblock %}
