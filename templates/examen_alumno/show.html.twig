{% extends 'base.html.twig' %}

{% block title %}ExamenAlumno{% endblock %}

{% block body %}
    <h1>Mostrando Exámenes de Alumnos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ examen_alumno.id }}</td>
            </tr>
            <tr>
                <th>Nota</th>
                <td>{{ examen_alumno.nota }}</td>
            </tr>
            <tr>
                <th>Tomo</th>
                <td>{{ examen_alumno.tomo }}</td>
            </tr>
            <tr>
                <th>Folio</th>
                <td>{{ examen_alumno.folio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_examen_alumno_index') }}">Regresar</a>

    <a href="{{ path('app_examen_alumno_edit', {'id': examen_alumno.id}) }}">Editar</a>

    {{ include('examen_alumno/_delete_form.html.twig') }}
{% endblock %}
