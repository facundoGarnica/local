<form method="post" action="{{ path('app_examen_alumno_delete', {'id': examen_alumno.id}) }}" onsubmit="return confirm('Confirmar Eliminación');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen_alumno.id) }}">
    <button class="btn">Eliminar</button>
</form>
