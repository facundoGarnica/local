<form method="post" action="{{ path('app_telefono_delete', {'id': telefono.id}) }}" onsubmit="return confirm('Confirmar Eliminación');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ telefono.id) }}">
    <button class="btn">Eliminar</button>
</form>
