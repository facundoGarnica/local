{% extends 'base.html.twig' %}

{% block title %}Modalidad index{% endblock %}

{% block body %}
    <h1>Modalidad index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for modalidad in modalidads %}
            <tr>
                <td>{{ modalidad.id }}</td>
                <td>{{ modalidad.descripcion }}</td>
                <td>
                    <a href="{{ path('app_modalidad_show', {'id': modalidad.id}) }}">show</a>
                    <a href="{{ path('app_modalidad_edit', {'id': modalidad.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_modalidad_new') }}">Create new</a>
{% endblock %}
