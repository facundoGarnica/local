{% extends 'base.html.twig' %}

{% block title %}Modalidad{% endblock %}

{% block body %}
    <h1>Modalidad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modalidad.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ modalidad.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modalidad_index') }}">back to list</a>

    <a href="{{ path('app_modalidad_edit', {'id': modalidad.id}) }}">edit</a>

    {{ include('modalidad/_delete_form.html.twig') }}
{% endblock %}
