{% extends 'base.html.twig' %}

{% block title %}Modalidad{% endblock %}

{% block body %}
    <h1>Mostrando Modalidades</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ modalidad.id }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ modalidad.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modalidad_index') }}">Regresar</a>

    <a href="{{ path('app_modalidad_edit', {'id': modalidad.id}) }}">Editar</a>

    {{ include('modalidad/_delete_form.html.twig') }}
{% endblock %}
