{% extends 'base.html.twig' %}

{% block title %}Pais{% endblock %}

{% block body %}
    <h1>Mostrando Nacionalidades</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ pai.id }}</td>
            </tr>
            <tr>
                <th>Descripción</th>
                <td>{{ pai.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pais_index') }}">Regresar</a>

    <a href="{{ path('app_pais_edit', {'id': pai.id}) }}">Editar</a>

    {{ include('pais/_delete_form.html.twig') }}
{% endblock %}
