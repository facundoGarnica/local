<form method="post" action="{{ path('app_examen_final_delete', {'id': examen_final.id}) }}" onsubmit="return confirm('Confirmar Eliminación');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen_final.id) }}">
    <button class="btn">Eliminar</button>
</form>
