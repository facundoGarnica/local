<form method="post" action="{{ path('app_asistencia_delete', {'id': asistencium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asistencium.id) }}">
    <button class="btn">Delete</button>
</form>
