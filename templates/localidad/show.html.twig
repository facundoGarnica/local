{% extends 'base.html.twig' %}

{% block title %}Localidad{% endblock %}

{% block body %}
    <h1>Mostrando Localidades</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ localidad.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ localidad.nombre }}</td>
            </tr>
            <tr>
                <th>Código Postal</th>
                <td>{{ localidad.codigoPostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_localidad_index') }}">Regresar</a>

    <a href="{{ path('app_localidad_edit', {'id': localidad.id}) }}">Editar</a>

    {{ include('localidad/_delete_form.html.twig') }}
{% endblock %}
