{% extends 'base.html.twig' %}

{% block title %}Localidad index{% endblock %}

{% block body %}
    <h1>Listado de Localidades</h1>

    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombre</th>
                <th>Código Postal</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for localidad in localidads %}
            <tr>
                <td>{{ localidad.id }}</td>
                <td>{{ localidad.nombre }}</td>
                <td>{{ localidad.codigoPostal }}</td>
                <td>
                    <a href="{{ path('app_localidad_show', {'id': localidad.id}) }}">Mostrar</a>
                    <a href="{{ path('app_localidad_edit', {'id': localidad.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No se encontraron registros</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_localidad_new') }}">Incorporar Nueva Localidad</a>
{% endblock %}
